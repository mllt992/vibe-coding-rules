# BUG 记录模板

> 复制为 `docs/03_问题记录/yyyymmdd_BUG_xxx问题描述.md` 后填空。
> **不能复现的 BUG 不能"修复"**——先想办法复现。

---

## 元信息

- **BUG 编号**：BUG-yyyymmdd-xxx
- **标题**：（一句话描述：什么场景下出了什么问题）
- **报告人**：xxx
- **报告日期**：yyyy-mm-dd
- **严重级别**：P0 阻塞 / P1 主流程 / P2 次要 / P3 体验
- **状态**：待复现 / 已复现 / 修复中 / 已修复 / 已关闭 / 不处理
- **修复人**：xxx
- **关联功能/模块**：xxx

---

## 1. 现象描述

> 用客观语言描述发生了什么。**不要**写"页面坏了"——写"点击保存按钮后页面卡死，控制台报 500 错误"。

- **现象**：xxx
- **截图/录屏**：（链接或附件位置）
- **错误信息**：（前端报错文本 / 后端日志片段 / 错误码）

---

## 2. 复现步骤

> 编号化，确保他人按此能 100% 复现。如果不能稳定复现，写"复现率：x/10"并标记"偶现"。

1. 登录账号：xxx（角色：xxx）
2. 进入页面：xxx
3. 操作：xxx
4. 结果：出现 BUG

**前置数据**：
- 数据 1：xxx
- 数据 2：xxx

**环境**：
- 浏览器 / 客户端：xxx
- 服务端环境：本地 / 开发 / 测试 / 预发布 / 生产
- 时间：发生时间 yyyy-mm-dd hh:mm

---

## 3. 期望行为 vs 实际行为

| 维度 | 期望 | 实际 |
|---|---|---|
| 表现 | xxx | xxx |
| 数据 | xxx | xxx |
| 状态码 | 200 | 500 |
| 提示 | "保存成功" | （白屏） |

---

## 4. 影响范围

- **受影响功能**：xxx
- **受影响用户**：所有用户 / 仅 xxx 角色 / 仅 xxx 数据
- **是否阻塞业务**：是 / 否
- **是否已影响生产**：是 / 否
- **是否需要紧急修复**：是 / 否

---

## 5. 复现率与频次

- 复现率：x/10
- 首次发现：yyyy-mm-dd
- 出现频次：xxx
- 触发条件总结：xxx

---

## 6. 相关近期变更（如有）

- 近期相关代码改动：commit xxx
- 近期相关配置变更：xxx
- 近期相关数据变更：xxx

---

## 7. 根因分析

> 这一节是修复质量的核心。**未完成根因分析就开始改代码 = 大概率改错。**

### 7.1 排查过程
- 检查点 1：xxx → 结果 xxx
- 检查点 2：xxx → 结果 xxx
- 检查点 3：xxx → 结果 xxx

### 7.2 根因结论
> 一句话说清根因。

xxx 函数在 xxx 条件下，xxx 字段被设置为 xxx，导致后续 xxx。

### 7.3 触发条件
- 数据：xxx
- 角色：xxx
- 并发：xxx
- 时机：xxx

### 7.4 为什么之前没发现
- 代码视角：xxx
- 测试视角：xxx
- 上线流程视角：xxx

### 7.5 相似代码排查（grep 结果）
- 文件 A：存在类似问题，需要一并修
- 文件 B：检查过，不受影响

---

## 8. 修复方案

### 8.1 修复策略
- [ ] 直接修复
- [ ] 迂回修复（说明为什么不直接改）
- [ ] 临时修复 + 长期方案

### 8.2 具体改动
- 改动文件 A 的 xxx 函数：xxx
- 改动文件 B 的 xxx 函数：xxx
- 新增 xxx 校验

### 8.3 可能的副作用
- 副作用 1：xxx，影响 xxx
- 副作用 2：xxx，影响 xxx

### 8.4 数据修复脚本（如需）
```sql
-- 先 SELECT 验证范围
SELECT COUNT(*) FROM xxx WHERE 异常条件;

-- 修复
BEGIN;
UPDATE xxx SET yyy = zzz WHERE 异常条件;
-- 确认影响行数与预期一致后 COMMIT，否则 ROLLBACK
COMMIT;
```

### 8.5 用户确认
- 开发者已确认（yyyy-mm-dd hh:mm）：xxx

---

## 9. 验证

### 9.1 BUG 原现象验证
- [ ] 按原复现步骤操作 → 现象消失

### 9.2 边界变种验证
- [ ] 边界 1：xxx → 正常
- [ ] 边界 2：xxx → 正常
- [ ] 并发场景：xxx → 正常

### 9.3 相邻功能回归
- [ ] 功能 A：正常
- [ ] 功能 B：正常

### 9.4 同模式代码验证
- [ ] 文件 A 已一并修复并验证
- [ ] 文件 B 不受影响

---

## 10. 回归用例（永久保留）

> 同时拷贝一份到 `docs/05_回归用例/<模块>/回归用例.md`。

| 编号 | 前置条件 | 操作步骤 | 预期结果 | 优先级 |
|---|---|---|---|---|
| TC-REG-XXX-001 | xxx | xxx | xxx | P0 |

---

## 11. 修复结论

- **根因**：xxx
- **修复方式**：xxx
- **影响范围**：xxx
- **是否同类排查**：是，已排查 X 处
- **是否需要数据修复**：是 / 否，已执行
- **是否影响用户操作方式**：是 / 否
- **用户手册是否更新**：已更新 / 不需要
- **PR 链接**：xxx
- **上线时间**：xxx

---

## 12. 后续动作

- [ ] 写回归用例归档
- [ ] 更新用户手册（如有变化）
- [ ] 数据修复 SQL 已执行（如有）
- [ ] 字典 / 菜单 / 权限同步（如有变化）
- [ ] 通用陷阱清单（`docs/90_最佳实践/通用陷阱清单.md`）是否需要新增一条
