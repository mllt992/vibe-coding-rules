# 代码审查报告 · 用户登录功能（已填写样例）

> 这是一份**已填写**的代码审查报告样例，对应 `_示例_实施方案_用户登录.md` 的实现。
> 复制时**删除本说明行**与文档名中的 `_示例_` 前缀。

---

## 元信息

- **审查对象**：PR #142 - 新版用户登录功能
- **审查类型**：AI 自审 + 同伴 Review（开发者）
- **审查者**：AI（一审）→ 开发者李四（二审）
- **审查日期**：2026-05-13
- **关联需求/BUG**：需求 `docs/01_需求分析/20260510_用户登录需求分析.md`
- **变更规模**：+1842 / -612 行，影响 28 个文件（后端 12 / 前端 14 / 迁移 2）

---

## 1. 审查概要

- **整体结论**：⚠️ 修复后通过
- **必修问题**：3 项
- **建议改进**：5 项

---

## 2. 维度一：正确性

### 业务正确
- [x] 完整实现需求 §6 的 F-01~F-06 全部 6 项功能
- [x] 验收标准 11 条已对照（详见测试报告）
- [x] 边界条件：失败 0/1/2/3/4/5/6 次行为均测试通过
- [x] 异常路径：Redis 挂、验证码服务挂、并发已设计降级
- [x] 错误提示统一性：用户不存在 vs 密码错误响应完全一致 ✓

### 代码正确
- [x] 无 NPE 风险：`findByUsernameOrEmail` 返回 Optional，调用方处理
- [x] 无死循环 / 无限递归
- [x] 资源释放：Redis client 由 Spring 管理 ✓
- [x] 数值精度：失败计数为 int，锁定时间用 Instant ✓

### 数据正确
- [x] 事务边界：登录流程无事务（无 DB 写入需事务保护）；解锁接口在事务中
- [x] 外部调用未卷入 DB 事务 ✓
- [x] 业务唯一性：username 在 user 表已有唯一索引 ✓
- [x] 并发：用 Redis `INCR` 原子操作 ✓

### 发现的问题

| 编号 | 严重 | 位置 | 问题描述 | 建议 |
|---|---|---|---|---|
| 1 | **阻断** | AuthService.java:142 | 锁定逻辑使用 `==` 而非 `>=` 比较失败次数。竞态下可能跳过锁定 | 改为 `>=` |
| 2 | 重要 | LoginPage.vue:88 | 兜底 `router.replace('/')` 在 if 块外，可能覆盖 redirect 跳转（**未在审查中发现，后续上线被 QA 报 BUG-20260520-001**） | （审查盲点，已记入复盘） |

---

## 3. 维度二：规范性

- [x] **命名**：类、方法、字段符合 Java/TS camelCase 规范
- [x] **API 设计**：`/api/v1/auth/login` 符合 §02 规范（kebab + 复数 / 资源化）
- [x] **数据库**：字段命名 snake_case ✓；新增字段有默认值 ✓
- [x] **前端**：组件 PascalCase、props camelCase ✓
- [x] **后端**：Controller 薄、Service 厚 ✓
- [x] **错误处理**：用统一 BusinessException + 全局异常处理器 ✓
- [x] **Git 提交**：5 个原子提交，subject 清晰

### 发现的问题

| 编号 | 严重 | 位置 | 问题描述 | 建议 |
|---|---|---|---|---|
| 3 | **阻断** | AuthService.java:88 | 日志直接打印了 `principal`（可能含用户名/邮箱完整字符串），需脱敏 | 邮箱按 `t***@example.com` 脱敏；用户名截取前 3 字符 + `***` |
| 4 | 重要 | LoginRequest.java | 缺 `@Size(min=6, max=64)` for password | 加上注解 |
| 5 | 建议 | CaptchaInput.vue:23 | 注释 "// 这里加载验证码" 是废话 | 删除注释 |

---

## 4. 维度三：可维护性

- [x] **单一职责**：AuthService 仅处理认证；TokenService 仅处理 token
- [x] **重复代码**：登录失败处理被复用，无重复
- [x] **过度设计**：未引入不必要的抽象（如 strategy / factory）
- [x] **魔法值**：阈值常量化（`FAIL_COUNT_FOR_CAPTCHA = 3`、`FAIL_COUNT_FOR_LOCK = 5`、`LOCK_DURATION_MINUTES = 15`）
- [x] **依赖方向**：Controller → Service → Repository ✓
- [x] 无 console.log / debug 残留

### 发现的问题

| 编号 | 严重 | 位置 | 问题描述 | 建议 |
|---|---|---|---|---|
| 6 | 建议 | AuthService.java | 锁定时长常量直接写在 Service 中，建议提取到配置（application.yml） | 改为 `@Value("${auth.lock.duration-minutes:15}")` 方便调整 |
| 7 | 建议 | LoginPage.vue | 表单状态管理可提取到 composable | 当前规模可接受，留作 P3 |

---

## 5. 维度四：安全性

- [x] 登录接口在白名单（免登录），其他都鉴权
- [x] 解锁接口要求 `user:unlock` 权限 ✓
- [x] **越权场景测过**：普通用户调用 /api/v1/users/{id}/unlock → 403 ✓
- [x] 输入校验：`@Valid` + `@Size` + `@Pattern` 全部在后端
- [x] SQL：使用 JPA 参数化 ✓
- [x] **密码 BCrypt（cost=12）** ✓
- [x] **错误响应统一**：用户不存在与密码错误响应一致 ✓
- [x] **Token 用 HttpOnly + Secure + SameSite=Strict Cookie**
- [x] **限流**：单 IP 10次/分，单 username 20次/分 ✓
- [x] CSRF：API 用 Cookie + token 双重，已配置 SameSite

### 发现的问题

| 编号 | 严重 | 位置 | 问题描述 | 建议 |
|---|---|---|---|---|
| 8 | **阻断** | TokenService.java:67 | JWT 签发未限制 algorithm，**可被 `alg=none` 攻击** | 显式指定 `Jwts.SIG.HS256`，并在验签时校验 algorithm |

---

## 6. 跨视角检查

### 测试覆盖
- [x] 单元测试 23 条：AuthService 覆盖率 92%、TokenService 100%、CaptchaService 85%
- [x] 接口测试 15 条：对应测试用例 TC-AUTH-001~015，全部通过
- [x] 关键路径有自动化
- [x] 测试代码无空壳测试，断言明确

### 文档同步
- [x] 需求文档与代码一致
- [x] 实施方案文档已回填"实施结果"小节
- [x] 用户手册已新增：`docs/10_用户手册/20260513_用户登录功能说明.md`
- [x] 系统字典补充 `USER_STATUS.LOCKED`
- [x] 错误码字典补充 `USER_LOCKED`、`CAPTCHA_REQUIRED`、`CAPTCHA_INVALID`
- [x] OpenAPI 文档已同步生成
- [x] PR 描述完整（背景、改动、影响、测试）

### 可观测性
- [x] login 成功 / 失败均有日志
- [x] 锁定事件 WARN
- [x] 日志带 traceId
- [x] 监控埋点：登录成功率、P95 耗时、锁定计数

---

## 7. 审查 AI 产出代码的特殊关注

> 本 PR 主要由 AI 生成代码，专项审查：

- [x] **API 真实性**：BCrypt（`org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder`）、JJWT（`io.jsonwebtoken.Jwts`）已查官方文档确认 ✓
- [x] **逻辑跳跃**：业务规则的 6 个分支（OK / 密码错 / 用户不存在 / 已锁 / 已禁 / 需验证码）都真处理了 ✓
- [⚠️] **边界遗漏**：在问题 #1 中发现 `==` vs `>=` 这种典型 AI 边界遗漏
- [x] **置信度标注**：AI 在 PR 描述中标注了"BCrypt cost 12 选择基于 OWASP 推荐，中等置信"
- [x] **过度复杂**：方案未引入工厂模式 / 策略模式 / 不必要的抽象 ✓
- [⚠️] **注释自圆其说**：发现一处建议（问题 #5）
- [x] **测试不哑测**：测试用例断言明确，无 `assertNotNull` 凑数

---

## 8. 缺陷清单（汇总）

### 阻断（必修才能合并）
1. AuthService.java:142 `==` → `>=`（问题 #1）
2. AuthService.java:88 日志脱敏（问题 #3）
3. TokenService.java:67 JWT alg 限制（问题 #8）

### 重要（强烈建议修）
4. LoginRequest.java 密码 @Size 校验（问题 #4）

### 建议（可选改进）
5. 删除废话注释（问题 #5）
6. 锁定时长配置化（问题 #6）
7. LoginPage 表单 composable 提取（问题 #7 - P3 延后）
8. （问题 #2 在审查时未发现，已记入"审查盲点"复盘）

---

## 9. 审查结论与后续动作

### 结论
⚠️ **修复后通过** —— 必修 3 项与重要 1 项处理后可合并

### 后续动作
- [x] 作者修复阻断项（commit a3f7c2d）
- [x] 重要项 #4 已修复（commit 8b9d1e0）
- [x] 建议项 #5、#6 已修复
- [x] 修复后再次自审通过
- [x] CI 已绿
- [x] 已合并到 main（2026-05-13 18:30）

---

## 10. 审查者反思

- **审查盲点**：问题 #2（前端跳转兜底）在审查中未被发现，导致 5 月 20 日 QA 报 BUG。
- **教训**：审 AI 写的前端代码时，要专门关注"看起来合理但分支覆盖不全"——这次正是这类问题。
- **应升级到规范**：把"看 if 外是否有兜底语句覆盖了条件分支结果"加入 `103_代码审查工作流程.md` §"审查 AI 产出代码的特殊关注点" → 已追加。
- **应升级到陷阱清单**：把"前端登录跳转兜底代码与条件分支并存"加入 `docs/90_最佳实践/通用陷阱清单.md` §三 → 已追加。
